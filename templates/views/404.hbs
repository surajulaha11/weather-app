<!DOCTYPE html>
<html>
    <head>
        <link rel="stylesheet" href="/css/styles.css">
        <body>            
                {{>header}}
                <p>{{errorMessage}}</p>
                {{>footer}}
        </body>
    </head>
</html>